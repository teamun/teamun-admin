teamunApp.controller('DashboardCtrl', ['$scope', '$location', 'toaster', 'DashboardService', function($scope, $location, toaster, DashboardService) {

  // DashboardService.counts.get({}, function(data) {
  //   $scope.userCount = data.userCount;
  //   $scope.teamCount = data.teamCount;
  //   $scope.activityCount = data.activityCount;
  // }, function(error) {
  //   toaster.pop('error', error.data.message);
  // });

}]);
